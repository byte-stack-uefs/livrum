import InternalAccountLayout from "@/app/components/layouts/InternalAccountLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
    return <InternalAccountLayout>{children}</InternalAccountLayout>;
}
